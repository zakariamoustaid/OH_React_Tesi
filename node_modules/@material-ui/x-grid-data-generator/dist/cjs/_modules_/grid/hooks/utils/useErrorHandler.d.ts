import { GridApiRef } from '../../models/api';
import { GridComponentProps } from '../../GridComponentProps';
export declare function useErrorHandler(apiRef: GridApiRef, props: Pick<GridComponentProps, 'error'>): void;
