import { Logger } from '../../models/logger';
import { GridComponentProps } from '../../GridComponentProps';
export declare function useLoggerFactory(apiRef: any, props: Pick<GridComponentProps, 'logger' | 'logLevel'>): void;
export declare function useLogger(name: string): Logger;
