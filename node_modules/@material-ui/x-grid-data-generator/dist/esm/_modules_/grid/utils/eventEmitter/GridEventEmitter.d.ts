import * as React from 'react';
import { EventEmitter } from './EventEmitter';
export declare type GridListener = (params: any, event?: React.SyntheticEvent) => void;
export declare type GridSubscribeEventOptions = {
    isFirst?: boolean;
};
export declare class GridEventEmitter extends EventEmitter {
    /**
     * @ignore - do not document.
     */
    on(eventName: string, listener: GridListener, options?: GridSubscribeEventOptions): void;
}
